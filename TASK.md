<!--nav-->
# 모의 과제 1 — 옴니채널 재고 예약 API

> 📍 `3_실습_과제/실습1_재고동시성` · 진행 지시는 `4_실습_가이드/DAY2_도메인과_검증훈련.md` · `DAY3_1차_풀리허설.md` · 답안은 `5_실습_답안/실습1_결함_해답.md`

---

> **제한 시간 90분.** 타이머를 켜고 시작한다.
> 이 과제는 실제 시험을 모사한다. **일부러 모호하게 썼다.** 모호함을 해소하는 것도 과제다.
> 답을 보기 전에 반드시 스스로 해볼 것 → `5_실습_답안/실습1_결함_해답.md`

---

## 배경 (시나리오 에이전트 역할의 브리핑)

올리브영 커머스 플랫폼팀입니다.

다음 달 **올영세일**을 앞두고 재고 예약 서비스의 신뢰성 점검이 필요합니다.
현재 코드는 지난 스프린트에 급하게 작성되었고, 스테이징에서는 문제없이 동작합니다.
하지만 작년 세일 때 **"결제는 됐는데 재고가 없다"** 는 CS가 다수 접수됐습니다.

이번 작업의 목표는 이 서비스가 세일 트래픽을 견딜 수 있게 만드는 것입니다.

## 요구사항 (원문 그대로)

1. 고객이 상품을 장바구니에서 주문할 때, 결제 전에 **재고를 예약**할 수 있어야 합니다.
2. 결제가 완료되면 예약이 **확정**되고 실제 재고가 차감됩니다.
3. 결제하지 않고 이탈한 고객의 예약은 **일정 시간 후 풀려야** 합니다.
4. 온라인 물류센터 재고와 매장 재고를 **구분**해서 조회할 수 있어야 합니다.
5. 네트워크 문제로 클라이언트가 요청을 재전송해도 **문제가 없어야** 합니다.
6. 세일 피크 트래픽에서도 **재고보다 많이 팔리면 안 됩니다.**

## 제공된 것

`src/` 아래에 기존 구현이 있습니다. 컴파일되고, 기존 테스트 3개는 통과합니다.

> ⚠️ **먼저 `SETUP.md` 를 읽고 빌드 환경을 준비할 것.** (Gradle wrapper 생성 1회)
> 빌드가 안 도는 상태로 시작하면 PostToolUse 훅도 죽어 있어 실습의 절반이 무의미해진다.

## 제출물

- 동작하는 코드
- `docs/requirements.md` — 요구사항 표 (MUST/SHOULD/COULD/WON'T)
- `docs/ADR.md` — 설계 의사결정 3건 이상
- `docs/verification-report.md` — 검증 결과
- `docs/gaps.md` — 알려진 한계
- `README.md` — 실행 방법 + 아키텍처 + 트레이드오프

---

## ⏱ 진행 대본 (이대로 해볼 것)

### 0~10분 · 부트스트랩
```bash
bash ../../2_시험장_반입파일/bootstrap.sh .
cat ../../1_시험_가이드/03_부트스트랩_대본.md   # 순서 확인
claude
```

### 10~25분 · 요구사항 발굴
```
/kickoff TASK.md
```
그리고 **직접 생각해볼 것**: 위 요구사항 6개는 각각 무엇이 빠져 있는가?

<details>
<summary>힌트 (5분 고민 후 열 것)</summary>

- 1번: "예약"의 단위는? 주문 전체인가 라인별인가? 부분 재고만 있으면?
- 2번: 결제 완료 신호는 어디서 오나? 예약이 이미 만료됐으면?
- 3번: "일정 시간"이 몇 분인가? 만료 처리기는 누가 돌리나? 다중 인스턴스면?
- 4번: 구분해서 조회 + 합산 조회 둘 다? 매장은 몇 개까지 조회?
- 5번: "문제가 없다" = 멱등. 멱등키는 누가 만드나? 유효기간은?
- 6번: 이게 진짜 요구사항이다. 나머지 5개는 이걸 위한 수단이다.
</details>

### 25~40분 · 결함 발견 (이 과제의 핵심)
```
@src 전체를 읽고 adversarial-reviewer 서브에이전트로 검토하라.
이어서 @../../1_시험_가이드/05_엣지케이스_체크리스트.md 의 A(동시성), B(멱등성) 절을 순회 점검하라.
```
**목표: 결함 8개 이상 발견.** 5개 이하면 프롬프트가 약한 것이다.

### 40~75분 · 수정 + 테스트
```
test-designer 서브에이전트로 다음 불변식의 테스트를 먼저 작성하라 (red 확인 필수):
1. 동시 300 요청에서 재고 100개면 정확히 100건만 성공
2. 같은 멱등키 2회 호출 시 예약은 1건만 생성
3. 만료된 예약은 가용재고로 복원된다
4. 매장 재고와 온라인 재고는 서로 차감되지 않는다
그 다음 이 테스트가 green이 되도록 구현을 고쳐라.
```

### 75~90분 · 검증 + 패키징
```
/verify
/handoff
```

---

## 자가 채점 기준

| 항목 | 배점 | 확인 |
|---|---|---|
| 동시성 오버셀 방지 + **동시 출발 강제 테스트** 존재 | 20 | |
| 멱등성 (DB unique 제약 기반) + 재요청 시 동일 응답 | 15 | |
| 예약 TTL 만료 회수 + 다중 인스턴스 안전성 고려 | 15 | |
| 재고 소스(온라인/매장) 분리 | 10 | |
| 계층 분리, 엔티티 미노출, 에러 체계 | 10 | |
| ADR 3건 이상, 각각 대안·트레이드오프 포함 | 15 | |
| verification-report 에 실제 실행 출력 첨부 | 10 | |
| gaps.md 에 정직한 한계 기록 | 5 | |

**70점 미만이면 Day 4에 재도전.**
